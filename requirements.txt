# ── Core LLM / RAG dependencies ──────────────────────────────────────────────
chromadb>=1.5.9          # VectorStore (core/chroma_db/vector_store.py)
PyPDF2>=3.0.1            # PDF text extraction (core/chroma_db/pdf_processor.py)

# ── Dashboard (Flask web UI) ──────────────────────────────────────────────────
flask>=3.1.3             # Web server (dashboard/app.py)
markdown>=3.10.2         # Server-side Markdown → HTML (dashboard/app.py)

# ── Dev / test utilities (optional) ──────────────────────────────────────────
# Uncomment if you add pytest-based tests:
# pytest>=8.0.0
