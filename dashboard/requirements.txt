# dashboard/requirements.txt
# Install from the project root using the existing virtual environment:
#   .\.venv\Scripts\pip.exe install -r dashboard/requirements.txt

flask>=3.1.0
markdown>=3.6          # Server-side Markdown → HTML (tables, fenced code, etc.)

# Already required by the core pipeline (listed here for completeness):
chromadb>=0.5.0
PyPDF2>=3.0.0
