# dashboard/requirements.txt
# Install from the project root using the existing virtual environment:
#   .\.venv\Scripts\pip.exe install -r requirements.txt
#   .\.venv\Scripts\pip.exe install -r dashboard/requirements.txt
#
# All dependencies (chromadb, PyPDF2, flask, markdown) are already declared
# in the root requirements.txt — this file exists only for documentation.

flask>=3.1.3
markdown>=3.10.2
